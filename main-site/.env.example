# Server
PORT=4000
# Optional custom DB path
# DB_PATH=../database/db.sqlite

# Stripe (server)
# Required for live card payments
# Optional webhook secret
# STRIPE_WEBHOOK_SECRET=whsec_replace_me

# SMTP (ticket email delivery)
# Choose either SMTP_SERVICE OR SMTP_HOST/SMTP_PORT
# SMTP_SERVICE=gmail
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=absolute.cinema2027@gmail.com
SMTP_PASS=202723
SMTP_FROM=absolute.cinema2027@gmail.com
# SMTP_SECURE=true

# Auth/admin defaults (optional overrides)
# ADMIN_LOGIN_EMAIL=absolute.cinema2027@gmail.com
# ADMIN_LOGIN_PASSWORD=absolute2027
# ADMIN_LOGIN_USERNAME=Absolute Cinema Admin

# Ticket-pass token secret (change for production)
# TICKET_PASS_SECRET=replace-with-random-secret

# Client (Vite)
# Required for Stripe Elements on frontend
